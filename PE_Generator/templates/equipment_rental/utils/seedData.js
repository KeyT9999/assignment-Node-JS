require('dotenv').config(); const mongoose = require('mongoose'); const bcrypt = require('bcryptjs'); const connectDB = require('../config/db'); const User = require('../models/userModel'); const Equipment = require('../models/equipmentModel');
(async () => { try { await connectDB(); await Promise.all([User.deleteMany(), Equipment.deleteMany()]); await User.create([{ username: 'admin1', password: await bcrypt.hash('__ADMIN_PASSWORD__', 10), role: 'admin' }, { username: 'user1', password: await bcrypt.hash('__CUSTOMER_PASSWORD__', 10), role: 'customer' }]); await Equipment.create([{ name: 'Canon EOS Camera', category: 'camera', pricePerDay: 200000, depositFee: 1000000, stockQuantity: 5 }, { name: 'Epson Projector', category: 'projector', pricePerDay: 300000, depositFee: 1500000, stockQuantity: 3 }]); console.log('Seed completed'); } catch (e) { console.error(e); process.exitCode = 1; } finally { await mongoose.connection.close(); } })();
