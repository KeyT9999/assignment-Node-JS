# Postman
Import `__PROJECT_NAME__.postman_collection.json`. Run Login Manager/Keeper/Auditor first. Replace `product_id`, `warehouse_1`, and `warehouse_2` using IDs returned by seed/database. Each protected request uses its correct role token.
