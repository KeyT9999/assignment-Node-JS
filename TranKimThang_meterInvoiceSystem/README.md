# TranKimThang_meterInvoiceSystem

Advanced offline-compiled MCR backend for `powerbill`. Run `npm install`, copy `.env`, then `npm start`. Sample roles: billing_manager, meter_reader, auditor. Test auth first, then master-data, workflow and report endpoints in rubric order.
