require('dotenv').config();const express=require('express'),db=require('./config/db');const app=express();app.use(express.json());db();app.use('/auth',require('./routes/authRoutes'));app.use('/rooms',require('./routes/roomRoutes'));app.use('/reservations',require('./routes/reservationRoutes'));const PORT=process.env.PORT||9999;app.listen(PORT,()=>console.log(`Server on ${PORT}`));
